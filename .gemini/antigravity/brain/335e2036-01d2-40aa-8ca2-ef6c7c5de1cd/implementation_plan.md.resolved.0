# MOC 知識總索引整合方案 (Implementation Plan)

經過分析 `知識庫.MOC.md`、`知識庫索引.md` 與 `量子知識庫索引.md`，這三份文件存在高度的功能重疊，但它們各自代表了知識庫演進的不同階段：

1. **`知識庫.MOC.md`**：一份完全靜態的大綱筆記，主要列出傳統的領域分類（知識層級、學習路徑等），缺乏動態檢索能力。
2. **`知識庫索引.md`**：包含了非常複雜的 DataviewJS 實驗性代碼（如互動式探索工具、組合推薦系統）。有些語法（如使用 D3.js 渲染的佔位符）在 Obsidian 中其實無法直接透過標準 Dataview 執行。
3. **`量子知識庫索引.md`**：最高級且最完整的版本，完美定義了 `原子級`、`分子級`、`晶體級` 與 `超結構級` 的標籤架構，並包含了熱圖、陣列視覺化與分類檢索。但它也複製了一部份 `知識庫索引.md` 中的組合推薦系統程式碼，導致兩邊維護成本倍增。

## 🎯 整合目標
我們需要將這三者**合併為唯一的一份超級入口**（建議保留 `量子知識庫索引.md` 作為主體），並拔除無法運作的冗餘代碼，確保其高效、美觀且「零維護成本」。

## Proposed Changes (實作變更)

### 400知識/400-MOC總索引

#### [MODIFY] [量子知識庫索引.md](file:///d:\OneDrive\Obsidian\Navi%20Helios\400知識\400-MOC總索引\量子知識庫索引.md)
1. **吸收大綱**：將 `知識庫.MOC.md` 中的傳統「技能樹」與「方法論」等靜態分類，重新用 Dataview 語法改寫為動態區塊（根據 `parent` 或 `tags` 自動吸附），插入到量子索引的適合位置。
2. **清理無法運作的 JS 代碼**：移除檔案庫中純概念性（但沒有實作函數库支持）的 D3.js 網絡圖概念代碼。
3. **保留並強化核心檢索**：保留按「等級（原子到超結構）」與「應用領域（分析工具、決策框架等）」的動態表格。
4. **統一命名**：將此檔案設為 400 知識庫的唯一中央入口。

#### [DELETE] [知識庫.MOC.md](file:///d:\OneDrive\Obsidian\Navi%20Helios\400知識\400-MOC總索引\知識庫.MOC.md)
此檔案的分類目錄概念將被量子索引的 Dataview 取代，檔案本身將被刪除以防混淆。

#### [DELETE] [知識庫索引.md](file:///d:\OneDrive\Obsidian\Navi%20Helios\400知識\400-MOC總索引\知識庫索引.md)
此檔案中實用的部分（如聚類 Mermaid 圖表與推薦系統）已經與量子索引重疊，其餘未完成的實驗性代碼將被捨棄。檔案本身將被刪除。

## Verification Plan (驗證計畫)

### Manual Verification
1. 整合完成後，請使用者打開 `量子知識庫索引.md`。
2. 確認所有的 Dataview 表格與 DataviewJS 統計語法皆能正常渲染（沒有報錯）。
3. 確認原先的三份文件已整併為一份，且資訊沒有遺失。
