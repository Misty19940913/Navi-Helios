# Navi Helios 系統優化建議報告 (v1.0)

根據針對 `d:\OneDrive\Obsidian\Navi Helios` 系統結構、YAML 規範 (`610-100YAML規範.md`) 以及系統架構 (`610-020系統架構.md`) 的全域掃描與分析，目前系統尚未達到完美狀態，並有多處可以進行深度優化的空間。

以下是針對系統架構、效能、文件管理的優化診斷與具體建議：

## 1. 架構與命名一致性問題 (Architecture & Naming)

### 發現問題：
- **目錄編號衝突**：在 `300專案` 目錄下，存在多個相同編號的資料夾（如：`330人生航道`、`330個人理財`、`330個人金融知識庫`）。這破壞了編號系統的唯一性與排序邏輯。
- **系統架構定義落差**：根據 `610-020系統架構.md` 定義，任務池應為 `230任務池`，但實際建立的資料夾卻是 `200週期紀錄/240任務池`，導致指引文件與實體架構脫節。

### 優化建議：
- **重新編號/合併**：對 `300專案` 下的 `330` 系列資料夾進行重新編號（例如改為 330, 340, 350）或將其合併為單一的財富/人生主資料夾。
- **校準架構文件**：將 `240任務池` 更名回 `230任務池`，確保與說明文件完全一致。

## 2. YAML 規範與屬性遺失 (YAML Schema Violations)

### 發現問題：
- 系統強制要求遵循 **7行核心 YAML 結構**（包含 `tags`, `status`, `time_create`, `time_modifie`, `subject`, `parent`, `related`），但在抽樣檢查中發現嚴重缺失：
  - **完全缺乏 YAML**：例如 `400知識/健康.md` 等原子筆記/MOC 完全沒有 YAML 屬性。
  - **屬性空值/遺漏**：例如 `300專案/事業INDEX.md` 中，`parent` 與 `related` 皆為空值，且缺少了必須向上追溯的 `subject` 屬性。

### 優化建議：
- **批次修復 (Linter / Scripts)**：可使用 Obsidian Linter 插件或編寫腳本 (例如 PowerShell/Python)，為所有 `.md` 檔案強制補齊缺少的 YAML 基礎欄位。
- **嚴格執行父節點追溯**：根據 `610-230系統架構員` 守則，將所有的孤兒筆記（如 `400知識` 裡的鬆散筆記）補上 `parent` 連結到對應的 `MOC` 或 `100夢想`。

## 3. 根目錄冗餘與附件管理 (Root Clutter & Attachments)

### 發現問題：
- **根目錄污染**：Vault 根目錄散落了大量的非核心文件，例如多張圖片 (`.jpg`, `.png`, `.webp`)、腳本 (`restructure.ps1`)、設定檔 (`transactions.ledger`) 以及版型示範 (`gemini模板.md`)。
- **附件四散**：`000捕獲` 裡面混雜了圖片 (`.jfif`, `.jpeg`) 和 PDF 等未整理的附件。

### 優化建議：
- **建立全局附件資料夾**：在 `600支援/` 下建立專屬的資源目錄（如 `690Attachments`），並修改 Obsidian 設定將所有新貼上的圖片預設存入此處。
- **清理根目錄**：將根目錄的腳本與模板移至 `600支援/` 相對應的資料夾中（例如 `Template` 或 `管理協議/scripts`），確保根目錄僅留下 `Home.md` 等儀表板入口。

## 4. 目錄扁平化與孤兒節點 (MOC Check)

### 發現問題：
- `400知識` 與 `300專案` 中存在許多直接放置的 `.md` 檔案（如 `事業.md`, `人際.md`），若這些是 MOC (Map of Content)，應該明確在檔名加上 `.MOC` 結尾（如已有的 `知識庫.MOC.md`），以利於 Dataview 篩選與辨識。

### 優化建議：
- 將分類主幹筆記統一更名為 `[檔名].MOC.md` 或在 YAML 中標記 `tags: [類型/MOC]`，以構建強健的關聯網絡。

---

**下一步行動確認**：
請問是否需要我針對上述的任何一項（例如：自動幫您移動根目錄的圖片、重新編號 330 目錄、或是為沒有 YAML 的檔案補上空缺的屬性模板）開始執行修復？
