# AI 提示詞整合優化分析報告

## 現況診斷

您目前在 4 個不同位置維護著功能高度重疊的 AI 配置，每次系統更新都需要多處同步，容易產生版本分歧。

---

## 各工具現況盤點

| 工具 | 配置位置 | 數量 | 核心角色 |
|--|--|--|--|
| **Gemini CLI** | `610管理協議/gemini/` | 16 個檔案 | 中央調度中心 (GEMINI.md) + 角色人格 + 技能 |
| **Antigravity (本工具)** | `610管理協議/Agent/` | 1 AGENTS.md + 3 Prompts | 金庫脈絡說明 + 角色提示詞 |
| **Cursor** | `.cursor/rules/` | 4 個 .mdc | 2 個全域規則、2 個情境規則 |
| **Obsidian Copilot** | `610管理協議/copilot/` | 15 個獨立提示詞 | 通用文字操作（縮短、翻譯、摘要等）|

---

## 重疊分析：哪些內容在重複維護？

### 🔴 高度重疊（在 3-4 個地方都有）
1. **系統架構說明**：資料夾結構（000-700）、PARA 體系、知識流動路徑
   - Gemini `610-020系統架構.md`
   - Cursor `project-structure.mdc`（幾乎完整複製）
   - Agent `AGENTS.md`（摘要版）
   
2. **YAML 規範**：7 行核心屬性、標籤分類系統
   - Gemini `610-100YAML規範.md`（主規範）
   - Cursor `yaml-rules.mdc`（pointer 文件，已指向主規範）
   - 建置指南 `YAML屬性建置指南.md`（範本展開版）

3. **使用者身份 / 語氣偏好**
   - Gemini `610-010個人核心形象清單.md`
   - Agent `AGENTS.md` - User Preferences 段落
   - Cursor `project-structure.mdc` - 回應風格要求段落

### 🟡 部分重疊（各有差異）
4. **角色定義**：
   - Gemini：完整的 V1 + V2 三層角色矩陣（4+ 個角色檔案）
   - Cursor `postig.mdc`：SentryPigeon 內容戰略官（功能型，非常好）
   - Agent Prompts `系統架構師.md`、`Human-3.0-Expert.md`：深度角色提示詞

### 🟢 各工具獨有（無重疊，保留即可）
5. **Cursor 獨有**：`invest-rules.mdc`（MQ5/Pine 交易策略編程規則，有 `globs` 限制）
6. **Copilot 獨有**：15 個通用文字操作提示詞（Clip Web Page, Translate, Emojify等）
7. **Gemini 獨有**：`610-200工作流指令.md`、`//` 指令系統、對話日誌協議

---

## 核心問題

Gemini 的 `610-000GEMINI.md` 與 Cursor 的 `project-structure.mdc` 實際上在做同一件事：**定義系統架構 + 使用規則**。但它們各自維護，已出現版本分歧（如資料夾路徑描述不一致）。

---

## 整合方案（三個選項供您選擇）

> [!IMPORTANT]
> 您偏好以 `Agent/` 為主。以下方案均以此為前提設計。

---

### 方案一：「單一真實來源」架構（推薦）

**核心邏輯**：以 `Agent/AGENTS.md` 作為**主規範入口**，其他工具全部變成**讀取指針（pointer）**。

```
Agent/AGENTS.md           ← 唯一主文件（擴充版）
  ├── 系統架構           ← 從 gemini/610-020 整合進來
  ├── 身份 & 語氣        ← 從 gemini/610-010 整合進來
  ├── YAML 規範指針       ← 指向 610-100YAML規範.md
  ├── 角色矩陣           ← 從 gemini/610-000 整合進來
  └── 功能角色 Prompts/ 
       ├── 系統架構師.md  ← 現有，保留
       ├── 內容主編.md    ← 從 gemini 遷移
       └── Human-3.0-Expert.md ← 現有，保留

.cursor/rules/
  └── project-structure.mdc  ← 簡化為「指向 Agent/AGENTS.md」
  └── yaml-rules.mdc         ← 現有，已是 pointer，無需改動 ✅
  └── invest-rules.mdc       ← 保留，Cursor 獨有功能 ✅
  └── postig.mdc             ← 遷移至 Agent/Prompts/ 後，此處保留 pointer
```

**優點**：更新一次，四處同步。
**缺點**：需要 Cursor 能讀取 `Agent/AGENTS.md`（Cursor 支援 `@file` 引用）。

---

### 方案二：「職責分工」架構

**核心邏輯**：保持目前的工具分離，但明確分工，減少重疊內容。

```
Agent/AGENTS.md          ← 只保留：系統脈絡說明（給 AI 看懂金庫用）
gemini/610-000GEMINI.md  ← 只保留：Gemini 專屬語氣 & // 指令系統
.cursor/rules/*.mdc      ← 只保留：Cursor 特有的 coding 相關規則
copilot/                 ← 保留現狀（獨立功能，無需整合）
```

各角色提示詞統一放在 `Agent/Prompts/`，各工具以 prompt 引用方式調用。

**優點**：侵入性最低，改動最少。
**缺點**：仍需在多個地方更新系統架構說明。

---

### 方案三：「最小化痛苦」快速處理

只解決**已發現的版本分歧問題**：
1. 刪除 `610管理協議/cursor/` 子資料夾（已是廢棄的草稿，不是 active rules）
2. 刪除 `gemini/610-020系統架構.md`，改在 `GEMINI.md` 中直接引用 Cursor 的 `project-structure.mdc`
3. `Agent/AGENTS.md` 加入指向 `GEMINI.md` 的連結

**優點**：工作量最小，1小時內完成。
**缺點**：沒有根本解決多版本問題。

---

## 建議執行順序（如果選方案一）

1. 擴充 `Agent/AGENTS.md` → 整合系統架構、使用者偏好
2. 將 `postig.mdc` 提示詞複製到 `Agent/Prompts/`（內容主編）
3. 刪除 `cursor/` 子目錄（設計草稿，不是 active 規則）
4. 簡化 `project-structure.mdc` → 縮短為指向 AGENTS.md 的 pointer
5. gemini 角色檔案遷移至 `Agent/Prompts/`
6. 清理 gemini 子目錄，保留唯一無法轉移的功能（日誌協議、// 指令規範）

---

## 我的建議

選**方案一**。因為您的目標是「不論用哪種 AI 工具都讀同一個檔案」，這正是方案一的設計原則。

**下一步請告訴我**：
- 選擇哪個方案？
- `cursor/` 子目錄的文件（Cursor Rules 設計總覽、Cursor Rules規範等）是否有使用？可以刪除嗎？
- Gemini CLI 目前是否還在活躍使用中？
---

## 方案四：Copilot 提示詞遷移與中文化

目前的 Copilot 提示詞具有收藏價值，但不再使用該插件。將其遷移至 `Agent/Prompts` 並翻譯為繁體中文。

### 遷移路徑與對齊
- **來源**：`610管理協議/copilot/copilot-custom-prompts/`
- **目標**：`610管理協議/Agent/Prompts/`

### 檔案對應表
| 原始檔案 (English) | 遷移後檔案 (繁體中文) |
|---|---|
| `Clip Web Page.md` | `網頁擷取.md` |
| `Clip YouTube Transcript.md` | `YouTube 逐字稿擷取.md` |
| `Summarize.md` | `內容摘要.md` |
| `Emojify.md` | `加入表情符號.md` |
| `Explain like I am 5.md` | `五歲小孩也能懂的解釋.md` |
| `Fix grammar and spelling.md` | `修正語法與拼字.md` |
| `Generate glossary.md` | `生成術語表.md` |
| `Generate table of contents.md` | `生成目錄.md` |
| `Make longer.md` | `內容擴充.md` |
| `Make shorter.md` | `內容精簡.md` |
| `Remove URLs.md` | `移除 URL.md` |
| `Rewrite as tweet thread.md` | `改寫為推文串.md` |
| `Rewrite as tweet.md` | `改寫為推文.md` |
| `Simplify.md` | `文字簡化.md` |
| `Translate to Chinese.md` | `翻譯為中文.md` |

### 執行策略
1. **中文化書寫**：不僅是翻譯，而是根據 Misty 的語氣（理性、直白、實戰導向）重新表述。
2. **保留技術指令**：如 Mermaid 指令、變數佔位符 `{}` 等需精確保留。
3. **移除 Copilot 專屬 Frontmatter**：遷移後的檔案將使用 Agent 系統的 YAML 格式。
