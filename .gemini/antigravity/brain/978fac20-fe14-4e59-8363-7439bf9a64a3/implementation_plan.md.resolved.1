# 原子化拆解與畫布轉化系統 (Atomization & Canvas System)

## 1. 目標
建立一套自動化工作流，將長篇筆記拆分為具備溯源能力的原子卡片，並在 Obsidian Canvas 中生成具備邏輯連線的全域視覺文章。

## 2. 核心邏輯架構

### A. 階段一：原子化拆解 (Atomization)
- **輸入**：指定之長篇筆記。
- **處理**：
    - **錨點注入**：在原始筆記每一段落後方檢查並添加唯一錨點（如 `^anchor1`），若已有錨點則重複使用。
    - **清單生成**：在同目錄建立 `{{原名}}-原子化清單.md`。
    - **AI 互動預測**：捨棄簡略的分類詞（如包含、關聯），改為描述 **「兩者如何具體互動」**。
- **輸出範本**：
    ```markdown
    # 📋 原子化拆解提案
    ## [[原子化標題]]
    - **核心說明**：[摘要段落大意]
    - **溯源連結**：[[原始筆記#^錨點]]
    - **AI 互動預測**：
        - 與 [[另一個原子筆記]] 的互動：[描述兩者間的能量流動、因果支撐或轉化機制，例如：A 提供了 B 的動機，而 B 則將 A 的價值具現化]
    ```

### B. 階段二：用戶校準 (Calibration) - *手動環節*
- 用戶於 Obsidian 內編輯清單內容，調整標題、關係或刪除不必要節點。

### C. 階段三：視覺化文章建構 (Visualization)
- **處理**：
    - **筆記建立**：讀取清單，在 `410原子化知識庫/` 建立對應 Markdown。
    - **YAML 賦能**：自動寫入 `parent`（連回原始筆記）與 `subject`。
    - **畫布操作**：
        - 建立 `text` 節點，內容為 `## [[標題]]\n[摘要]`。
        - **全域唯一實例**：若畫布已存在同名節點，強制連線至該節點，禁止重複。
        - **互動敘事連線**：連線 `edge` 的 `label` 必須攜帶清單定義的詳細互動說明，而非單一詞彙。
- **輸出**：同目錄下的 `{{原名}}.canvas`。

## 3. 待建立檔案

### [NEW] [.agents/skills/atomize-note/SKILL.md](file:///d:/OneDrive/Obsidian/Navi%20Helios/.agents/skills/atomize-note/SKILL.md)
定義原子化拆解與清單生成的提示詞邏輯。

### [NEW] [.agents/skills/canvas-visualizer/SKILL.md](file:///d:/OneDrive/Obsidian/Navi%20Helios/.agents/skills/canvas-visualizer/SKILL.md)
定義畫布佈署、全域實例檢查與標籤連線的處理邏輯。

### [NEW] [.agents/workflows/note-to-canvas.md](file:///d:/OneDrive/Obsidian/Navi%20Helios/.agents/workflows/note-to-canvas.md)
整合上述兩項 Skill 的標準作業流程指令。

## 4. 驗證計畫

### 自動化測試 (模擬執行)
1.  **測試錨點注入**：建立測試筆記，確認執行後段落末尾是否正確出現 `^` 錨點。
2.  **測試清單生成**：驗證 `{{原名}}-原子化清單.md` 内容格式是否完全符合規範。
3.  **測試畫布生成**：建立包含兩個節點的測試清單，驗證 `.canvas` 檔案中節點類型是否為 `text`，且邊線 label 是否正確。

### 手動驗證
1.  請使用者檢視產出的 `原子化清單.md` 格式是否滿意。
2.  請使用者開啟生成的 `.canvas` 檔案，檢查節點與連線的視覺呈現。
