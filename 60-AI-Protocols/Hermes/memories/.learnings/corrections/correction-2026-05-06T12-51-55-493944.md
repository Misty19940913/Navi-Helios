# Correction: User-Initiated Correction
**Session:** 20260506_124156_37fe7b
**Timestamp:** 2026-05-06T12:51:55.493944
**Correction Hash:** c3e2a78f48517c79

## User Said
[DreadxMisty] 你參照的模板是什麼? frontmatter規範不是一直存在`80-System/84-YAML規範`?還是你誤用原作者的規範?

## My Wrong Answer (that was corrected)
你的邏輯是對的。讓我直接說：

**`title` 欄位在你的體系下是冗餘的。**

你的檔案名稱本身就已經是 `ID_標題.md`，例如：
- `K-BIZ-001_客戶細分類型.md`
- `K-FIN-032_五大水池系統.md`

filename 承擔了 title 的功能，且 Obsidian 的搜尋、backlinks、Graph View 全都是基於 filename，而非 frontmatter 的 `title` 欄位。

### `title` 原本設計的用途是：
1. **連結顯示名稱** — Obsidian 可用 `title` 覆寫顯示名稱（但你有 `aliases` 專門處理這件事）
2. **搜尋索引** — 但 Obsidian 搜尋本來就索引 filename 和內容
3. **SEO / 匯出時的標題** — 但你不用 Obsidian 發布

**你的 filename 已經是 `title` 了，多寫一個等於重複輸入。**

---

## 重新計算真實合規率

如果拿掉 `title` 這個多餘欄位，實際合規狀況是：

| 問題 | 修正前 | 修正後 |
|------|--------|--------|
| 缺 `id` | 452 + 155 + 26 = **633** | **633** |
| 缺 `title` | 452 + 9 = **461** | → 廢除，歸零 |
| 缺 `tags` | 26 | **26** |
| 無 frontmatter | 15 | **15** |
| 無 `sources` | 263 + 15 = **278** | **278** |

**真正需要補的欄位只有三個：id、tags、sources。**

---

這三個欄位裡，`id` 是最關鍵的——你確定 `id` 對你的 workflow 有實質作用嗎？還是也只是從模板留下來的？

## What I Learned
（待蒸餾 — 需分析並寫入 atomic fact）

---
*此檔案由 correction_scanner.py 自動生成（user correction fingerprint）*
