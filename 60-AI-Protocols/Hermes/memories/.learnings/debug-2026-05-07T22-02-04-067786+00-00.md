# Debug: read_file / api_error
**Timestamp:** 2026-05-07T22:02:04.067786+00:00  
**Hash:** e1015a80c36c58e6  
**Session:** cron_fa865b73fa78_20260508_060105

## 錯誤摘要
API error in read_file: {"content": "     1|#!/usr/bin/env bun\n     2|/**\n     3| * PreCompact.hook.ts - Preserve Context Before Compaction (P

## 錯誤詳情
```
Error Type: api_error
Tool Args: {'path': '/home/misty/.hermes/Personal_AI_Infrastructure/Releases/v5.0.0/.claude/hooks/PreCompact.hook.ts'}

--- Error Output (last 30 lines) ---
{"content": "     1|#!/usr/bin/env bun\n     2|/**\n     3| * PreCompact.hook.ts - Preserve Context Before Compaction (PreCompact)\n     4| *\n     5| * PURPOSE:\n     6| * Captures critical session context before Claude Code compresses the\n     7| * conversation. Outputs a structured handover note that survives compaction,\n     8| * ensuring continuity of work-in-progress state, active decisions, and\n     9| * file context across the compression boundary.\n    10| *\n    11| * TRIGGER: PreCompact (both auto and manual)\n    12| *\n    13| * INPUT:\n    14| * - stdin: Hook input JSON (session_id, transcript_path)\n    15| * - Files: MEMORY/STATE/current-work*.json, active plans, task state\n    16| *\n    17| * OUTPUT:\n    18| * - stdout: Structured handover context (preserved through compaction)\n    19| * - stderr: Status messages\n    20| * - exit(0): Always (non-blocking)\n    21| *\n    22| * PERFORMANCE:\n    23| * - Non-blocking: Yes\n    24| * - Typical execution: <100ms\n    25| */\n    26|\n    27|import { existsSync, readFileSync, readdirSync } from 'fs';\n    28|import { join, basename } from 'path';\n    29|import { findArtifactPath } from './lib/isa-utils';\n    30|\n    31|const BASE_DIR = process.env.PAI_DIR || join(process.env.HOME!, '.claude', 'PAI');\n    32|const MEMORY_DIR = join(BASE_DIR, 'MEMORY');\n    33|const STATE_DIR = join(MEMORY_DIR, 'STATE');\n    34|const WORK_DIR = join(MEMORY_DIR, 'WORK');\n    35|\n    36|interface HookInput {\n    37|  session_id?: string;\n    38|  transcript_path?: string;\n    39|  cwd?: string;\n    40|}\n    41|\n    42|function readJSON(path: string): any {\n    43|  try {\n    44|    if (!existsSync(path)) return null;\n    45|    return JSON.parse(readFileSync(path, 'utf-8'));\n    46|  } catch {\n    47|    return null;\n    48|  }\n    49|}\n    50|\n    51|function readText(path: string): string | null {\n    52|  try {\n    53|    if (!existsSync(path)) return null;\n    54|    return readFileSync(path, 'utf-8').trim();\n    55|  } catch {\n    56|    return null;\n    57|  }\n    58|}\n    59|\n    60|function getCurrentWork(sessionId?: string): any {\n    61|  // Try session-scoped state first\n    62|  if (sessionId) {\n    63|    const scoped = join(STATE_DIR, `current-work-${sessionId}.json`);\n    64|    const data = readJSON(scoped);\n    65|    if (data) return data;\n    66|  }\n    67|  // Fall back to legacy global state\n    68|  return readJSON(join(STATE_DIR, 'current-work.json'));\n    69|}\n    70|\n    71|/**\n    72| * Read the active session's Ideal State Artifact (ISA.md, or legacy PRD.md).\n    73| * `slug` is the session directory name under MEMORY/WORK/.\n    74| */\n    75|function getActiveISA(slug: string): string | null {\n    76|  const path = findArtifactPath(slug);\n    77|
```

### 相關歷史錯誤
- [error-1] 2026-05-07T22:02:04.067786+00:00: API error in read_file: {"content": "     1|#!/usr/bin/env bun\n     2|/**\n     3| * PreCompact.hook.ts - Preserve Cont
- [error-1] 2026-05-07T22:02:04.066671+00:00: API error in read_file: {"content": "   501|Claude Code's built-in auto-memory system writes learnings to `~/.claude/pro
- [error-1] 2026-05-05T09:50:59.600691+00:00: API error in read_file: {"content": "   985|#   on_session_finalize, on_session_reset, subagent_stop\n   986|#\n   987|#

## 懷疑模式
（待分析 — 需人工審查）

## 驗證計劃
- [ ] 確認錯誤是否可重現
- [ ] 檢查 tool args 是否正確
- [ ] 查詢相關錯誤歷史

---
*此檔案由 self-learner plugin 自動生成*
