# Debug: read_file / tool_error
**Timestamp:** 2026-05-07T22:01:46.996180+00:00  
**Hash:** a5cd0b13a36658fd  
**Session:** cron_fa865b73fa78_20260508_060105

## 錯誤摘要
Tool error in read_file: {"content": "     1|#!/usr/bin/env bun\n     2|/**\n     3| * ISASync.hook.ts — Read-only ISA → work.json sync via PostT

## 錯誤詳情
```
Error Type: tool_error
Tool Args: {'path': '/home/misty/.hermes/Personal_AI_Infrastructure/Releases/v5.0.0/.claude/hooks/ISASync.hook.ts'}

--- Error Output (last 30 lines) ---
{"content": "     1|#!/usr/bin/env bun\n     2|/**\n     3| * ISASync.hook.ts — Read-only ISA → work.json sync via PostToolUse\n     4| *\n     5| * TRIGGER: PostToolUse (Write, Edit)\n     6| *\n     7| * v4.1.0 (PRD → ISA rename): the per-session artifact is now ISA.md.\n     8| * Sessions created before v4.1.0 still ship a PRD.md; this hook reads either,\n     9| * preferring ISA.md when both exist (legacy behavior — there should never be\n    10| * both for a single session).\n    11| *\n    12| * v3.2.0: Hooks are READ-ONLY from the artifact's perspective.\n    13| * The AI writes all ISA content directly (criteria, checkboxes, frontmatter).\n    14| * This hook ONLY reads the ISA and syncs to work.json for the dashboard.\n    15| *\n    16| * - Write/Edit on ISA.md (or legacy PRD.md) → read frontmatter + criteria → sync to work.json\n    17| */\n    18|\n    19|import { readFileSync, existsSync } from 'fs';\n    20|import {\n    21|  parseFrontmatter,\n    22|  syncToWorkJson,\n    23|  readRegistry,\n    24|  ARTIFACT_FILENAME,\n    25|  LEGACY_ARTIFACT_FILENAME,\n    26|} from './lib/isa-utils';\n    27|import { pushStateToTargets, pushEventsToTargets } from './lib/observability-transport';\n    28|import { setPhaseTab } from './lib/tab-setter';\n    29|import type { AlgorithmTabPhase } from './lib/tab-constants';\n    30|\n    31|let input: any;\n    32|try {\n    33|  input = JSON.parse(readFileSync(0, 'utf-8'));\n    34|} catch {\n    35|  process.exit(0);\n    36|}\n    37|\n    38|const toolInput = input.tool_input || {};\n    39|\n    40|async function main() {\n    41|  // Only trigger for ISA.md (or legacy PRD.md) files in MEMORY/WORK/\n    42|  const filePath = toolInput.file_path || '';\n    43|  if (!filePath.includes('MEMORY/WORK/')) return;\n    44|  const isISA = filePath.endsWith('/' + ARTIFACT_FILENAME) || filePath.endsWith(ARTIFACT_FILENAME);\n    45|  const isLegacyPRD = filePath.endsWith('/' + LEGACY_ARTIFACT_FILENAME) || filePath.endsWith(LEGACY_ARTIFACT_FILENAME);\n    46|  if (!isISA && !isLegacyPRD) return;\n    47|\n    48|  // Use the actual file path that was just written/edited, not findLatestISA()\n    49|  // findLatestISA() scans all artifacts by mtime and can return the wrong file\n    50|  // when multiple sessions exist or when a file's mtime is bumped by git ops.\n    51|  const isaPath = filePath;\n    52|  if (!existsSync(isaPath)) return;\n    53|\n    54|  const content = readFileSync(isaPath, 'utf-8');\n    55|  const fm = parseFrontmatter(content);\n    56|  if (!fm) return;\n    57|\n    58|  // Check existing phase before sync to detect phase changes\n    59|  const newPhase = (fm.phase || '').toUpperCase();\n    60|  let oldPhase = '';\n    61|  if (fm.slug) {\n    62|    try {\n    63|      const registry = readRegistry()
```

### 相關歷史錯誤
- [error-1] 2026-05-07T22:01:46.996180+00:00: Tool error in read_file: {"content": "     1|#!/usr/bin/env bun\n     2|/**\n     3| * ISASync.hook.ts — Read-only ISA →
- [error-1] 2026-05-07T20:31:32.637072+00:00: Tool error in read_file: {"content": "     1|---\n     2|name: Agents\n     3|description: \"Compose CUSTOM agents from 
- [error-1] 2026-05-07T22:01:38.442958+00:00: Tool error in read_file: {"content": "     1|#!/usr/bin/env bun\n     2|/**\n     3| * ContentScanner.hook.ts — PostTool

## 懷疑模式
（待分析 — 需人工審查）

## 驗證計劃
- [ ] 確認錯誤是否可重現
- [ ] 檢查 tool args 是否正確
- [ ] 查詢相關錯誤歷史

---
*此檔案由 self-learner plugin 自動生成*
