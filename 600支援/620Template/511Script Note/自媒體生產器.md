---
tags: [系統/支援]
status: DONE
time_create: 2026-01-17
time_modifie: 2026-01-17
related:
parent:
aliases: []
publish_platforms:
按讚: "0"
留言: "0"
轉發: "0"
分享: "0"
---
<%*
/* Navi Helios 3.5 - 冷峻主編創作引擎
*/

// 1. 獲取素材
let sourceMaterial = tp.file.selection();
if (!sourceMaterial || sourceMaterial.trim() === "") {
    sourceMaterial = await tp.system.prompt("請輸入或貼上創作素材");
}

if (!sourceMaterial) return;

// 2. 準備 API 參數
const API_KEY = "AIzaSyAaeb_7sH1HnMrbMCuxC-hxshqVW7jDd2g";
const MODEL = "gemini-2.5-flash";
const API_URL = `https://generativelanguage.googleapis.com/v1beta/models/${MODEL}:generateContent?key=${API_KEY}`;

// 3. 定義系統提示詞
const systemPrompt = `你現在是「內容主編」，遵守[[610-220內容主編]]。
人格定錨：[[610-010個人核心形象清單]]（理性、警察/交易者背景、底層邏輯控、黑膠咖啡場景）。
全域禁令：禁止結論式結尾、禁止溫情加油、禁止使用「希望、建議、一起」。

請針對以下素材執行 //media 指令，產出：
1. IG 圖文（具穿透力、場景感強）
2. Threads 專用高參與度短文(約50字)。
3. 15s 短影片腳本（含運鏡提示與環境音設定）

素材內容：
${sourceMaterial}`;

// 4. 執行 API 呼叫
let resultText = "";
try {
    const response = await fetch(API_URL, {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json'
        },
        body: JSON.stringify({
            contents: [{
                parts: [{ text: systemPrompt }]
            }]
        })
    });

    const data = await response.json();
    if (response.ok) {
        if (data.candidates && data.candidates[0].content) {
            resultText = data.candidates[0].content.parts[0].text;
        } else {
            resultText = "⚠️ API 回傳格式異常，請確認 API Key 是否正確。";
        }
    } else {
        // 修正：刪除末尾多出的反引號與分號錯誤
        resultText = `⚠️ API 錯誤: ${data.error ? data.error.message : '未知錯誤'} (代碼: ${data.error ? data.error.code : response.status})`;
    } 
} catch (e) {
    resultText = `⚠️ 系統連線失敗: ${e.message}`;
}

// 5. 強制獲取「現在這個檔案」的真正標題
const currentTitle = tp.file.title;

// 6. 設定輸出內容
const finalOutput = `
---
tags: [系統/創作]
status: DONE
time_create: ${tp.date.now("YYYY-MM-DD")}
time_modifie: ${tp.date.now("YYYY-MM-DD")}
related:
parent:
aliases:
publish_platforms:
按讚: "0"
留言: "0"
轉發: "0"
分享: "0"
---
# 創作紀錄 //media

## 📥 原始素材
${sourceMaterial}

---

${resultText}
`;

tR += finalOutput;
%>